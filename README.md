# hyperedge_prediction

This repository contains the current code for a hyperedge prediction training pipeline based on HyperGCN-style components and a subgraph sampling training strategy.

The README is a temporary placeholder. It will be replaced after the project PDF is provided.

## Current contents

- Core model and training pipeline implementation
- Subgraph sampling and negative sampling utilities
- Existing project notes in `README_subgraph_training.md`

## Notes

- Large generated artifacts, logs, caches, and local data files are excluded from version control.
- Some default dataset paths in the code still point to local development paths and may need cleanup before the repository is shared publicly.
